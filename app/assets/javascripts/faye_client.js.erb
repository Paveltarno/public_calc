// Creates a connection to the faye messaging server
$(function() {
  var faye = new Faye.Client("<%= "#{Rails.application.secrets.faye_url}.js" %>");
  faye.subscribe("calc/screen_changed", function(data) {
    $( ".screen" ).html(data);
  });
});