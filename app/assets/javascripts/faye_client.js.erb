// Creates a connection to the faye messaging server
$(function() {
  var faye = new Faye.Client("<%= Rails.application.secrets.faye_url %>");
  faye.subscribe("/calc/broadcast", function(data) {
    eval(data);
  });
});